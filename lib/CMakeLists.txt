# Compatibility stub.
#
# The canonical build entry point is the repository root `CMakeLists.txt`.
# This file is intentionally left empty so the legacy `lib/` folder does not
# look like a separate build project in Qt Creator.
