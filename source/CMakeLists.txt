# Compatibility stub.
#
# The canonical build entry point is the repository root `CMakeLists.txt`.
# This file is intentionally left empty so Qt Creator does not treat the
# `source/` folder as a separate project definition.
